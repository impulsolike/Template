<div style="display:none!important;">
    <h1>{{ $title ?? 'Template' }}</h1>
    <p>{{ $description ?? 'Description' }}</p>
    <img src="{{ $image ?? 'image' }}" alt="{{ $image_alt ?? 'Template' }}" />
</div>
